{
  "name": "member-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "P5.5 Member Management API — separate service/database from the main Supabase-backed app. See docs/phase-5-5/00-member-management-architecture.md.",
  "scripts": {
    "start": "node src/index.js",
    "migrate": "node scripts/migrate.mjs",
    "migrate:fresh": "node scripts/migrate.mjs --fresh",
    "seed:dev": "node scripts/seed-dev.mjs",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
